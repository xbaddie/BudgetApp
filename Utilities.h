#pragma once
#include <string>
#include <iostream>

using namespace std;

class Utilities
{
public:
    static string readLine();
    static char getCharacter();
    bool validateInput(string input);
};
